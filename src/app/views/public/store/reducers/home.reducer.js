const initialState = {
    searchData: {}
};

const homeReducer = function (state = initialState, action) {
    switch (action.type) {
        default :
            return state;
    }
};

export default homeReducer;
