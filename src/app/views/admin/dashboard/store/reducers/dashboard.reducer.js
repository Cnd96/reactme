const initialState = {
    searchData: {},
    pagedUsers: {}
};

const dashboardReducer = function (state = initialState, action) {
    switch (action.type) {
        default :
            return state;
    }
};

export default dashboardReducer;
